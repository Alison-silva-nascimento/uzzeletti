const current = location.pathname.split('/').pop() || 'index.html';
const links = [['index.html','Home'],['produtos.html','Produtos'],['colecoes.html','Coleções'],['novidades.html','Novidades'],['sobre.html','Sobre'],['contato.html','Contato']];
const header = document.querySelector('[data-header]');
const footer = document.querySelector('[data-footer]');
if(header) header.innerHTML = `<a class="skip-link" href="#conteudo">Pular para o conteúdo</a><header class="site-header"><div class="container nav-wrap"><a class="brand" href="index.html" aria-label="UZZELETTI — início"><img src="assets/logo/uzzeletti-logo.png" alt="UZZELETTI Moda Íntima"></a><button class="menu-toggle" aria-label="Abrir menu" aria-expanded="false">☰</button><nav class="nav" aria-label="Navegação principal">${links.map(([href,label])=>`<a href="${href}" ${current===href?'aria-current="page"':''}>${label}</a>`).join('')}<a class="header-cta" href="contato.html">Fale conosco</a></nav></div></header>`;
if(footer) footer.innerHTML = `<footer class="site-footer"><div class="container"><div class="footer-grid"><div><img class="footer-logo" src="assets/logo/uzzeletti-logo.png" alt="UZZELETTI"><p class="muted">Moda feminina para vestir presença, beleza e autenticidade.</p></div><div><p class="footer-title">Navegue</p><div class="footer-links">${links.slice(1).map(([h,l])=>`<a href="${h}">${l}</a>`).join('')}</div></div><div><p class="footer-title">Atendimento</p><div class="footer-links"><span>WhatsApp: em breve</span><span>Instagram: em breve</span><span>Horários: em breve</span></div></div></div><div class="footer-note"><span>© ${new Date().getFullYear()} UZZELETTI. Todos os direitos reservados.</span><span>Site institucional e catálogo — compras por contato.</span></div><p class="developer-credit">Desenvolvido por Alison Silva Do Nascimento Web Devoloper</p></div></footer><button class="back-top" aria-label="Voltar ao topo">↑</button><a class="whatsapp-float" href="contato.html" aria-label="Contato pelo WhatsApp">W</a>`;
const toggle=document.querySelector('.menu-toggle'), nav=document.querySelector('.nav');
toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',open);toggle.textContent=open?'×':'☰';document.body.classList.toggle('menu-open',open)});
nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');document.body.classList.remove('menu-open')}));
const topBtn=document.querySelector('.back-top');addEventListener('scroll',()=>topBtn?.classList.toggle('visible',scrollY>600));topBtn?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
document.querySelectorAll('img').forEach(img=>{if(!img.hasAttribute('loading')&&!img.closest('.hero'))img.loading='lazy'});
